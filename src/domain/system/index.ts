import type { DomainDefinition } from '@domain';

export default {
	id: 'system',
	name: 'System',
	description: 'General bot utilities, moderation controls, and housekeeping jobs.'
} satisfies DomainDefinition;
