import type { DomainDefinition } from '@domain';

export default {
	id: 'announcements',
	name: 'Announcements',
	description: 'Server announcements and announcement dispatch scheduling.'
} satisfies DomainDefinition;
