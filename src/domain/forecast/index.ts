import type { DomainDefinition } from '@domain';

export default {
	id: 'forecast',
	name: 'Forecast',
	description: 'Forecast subscriptions, delivery jobs, and message controls.'
} satisfies DomainDefinition;
