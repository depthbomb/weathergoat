import type { DomainDefinition } from '@domain';

export default {
	id: 'radars',
	name: 'Radars',
	description: 'Radar commands, subscriptions, and auto-update workflows.'
} satisfies DomainDefinition;
